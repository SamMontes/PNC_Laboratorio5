spring:
  application:
    name: Laboratorio3

  datasource:
    url: jdbc:postgresql://localhost:5432/Specimens
    username: postgres
    password: LunaMoka2001

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
